FUNCTION dot_product(v1:array ,v2:array) : INTEGER
VAR
    
BEGIN
    
    RETURN v1[0]*v2[1] + v2[0]* v1[1];
END
ALGORITHM dot_product
VAR
   a,b,c,d:INTEGER ; 
   
BEGIN
FOR c FROM  0 TO b-1 STEP 1  DO
FOR d FROM 0 TO b-2 STEP 1  DO
a:=dot_product(tab(d),+ tab (d+1))
IF (x:=0) THEN
    write( vectors are orthogonal )
END_IF

   
END_FOR 
END_FOR  
END